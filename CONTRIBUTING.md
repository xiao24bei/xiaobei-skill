# Contributing

欢迎改进小北在读研的开源 Skill 合集。

## 提交前检查

- 不要提交 `__pycache__/`、`.pyc`、`.pptm`、`.bas`、用户原始图片或未经审核的生成文件。仓库维护者明确授权并完成隐私/版权检查的最小公开案例除外。
- 不要提交未授权论文图、商标、截图、显微图或商业素材。
- 修改 `SKILL.md` 时，保持 workflow 清晰，不要把大量示例堆进主文件；复杂说明放到 `references/`。
- 修改脚本后，至少运行一次对应脚本的 `--help` 或最小 smoke test。
- 如果改动了依赖，更新对应子 Skill 的 `requirements.txt`；必要时同步根目录聚合依赖。
- 新增 Skill 时同步更新 `skill-catalog.json`，且 `name`、目录名与 `agents/openai.yaml` 中的展示信息必须一致。
- 所有展示名统一以“`小北在读研 · `”开头；内部 Skill 名使用小写英文、数字和连字符。

## Pull Request 建议

请说明：

- 改了什么；
- 为什么需要改；
- 影响哪个 Skill、PowerPoint/WPS 或操作系统路径；
- 是否引入新的依赖或安全风险。

## 品牌说明

你可以 fork、修改和分发代码，但请不要把修改版描述成“小北在读研官方版本”，也不要使用“小北在读研”的个人品牌来暗示作者背书。
